# ============ 依赖 / 临时产物 ============
node_modules/
_work/
_tmp/

# ============ 私密信息：绝不提交 ============
# 订阅链接、证书私钥、口令等一律不入库。
# 注意：以下按「模式」忽略，而不是按具体路径，这样目录重组后依然生效。
**/.raw/
**/*.raw/

*.local.conf
local.secret
*.p12
*.mobileconfig
*.crt
*.key
*.pfx
_wk/

# 插件包临时文件
*.plugin

# 临时分析文件（下划线开头）
_*.list
_*.js
_*.mjs
_*.json
_*.conf
_*.snippet
_*.png
# 例外：kelee 转换器的辅助产物**必须入库**（见文末说明）。注意 git 是「后面的规则覆盖前面」，
# 所以这些 `!` 必须紧跟在 `_*` 模式之后。
!QuantumultX/rules/kelee/_exclusions.json
!QuantumultX/rules/kelee/_conversion-report.json
!QuantumultX/rules/kelee/_hostnames.conf

# 本地临时/脚本产物（不属于仓库内容）
__pycache__/
*.pyc
*.tmp
_check.py

# kelee 转换器的辅助产物必须入库（例外规则在上面 `_*` 之后）：
# CI 流程是 convert-plugins 先跑、fetch-snapshot 后跑并**读取 _exclusions.json**
# 来排掉与 kelee 重叠的规则。若它被 `_*.json` 忽略、没入库，
# CI 上「kelee 胜出」就完全不生效（实测就是这个原因，重复处理照样存在）。

# CI 序列的临时模拟目录
_sim/
