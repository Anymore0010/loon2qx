# ============ 依赖 / 临时产物 ============
node_modules/
_work/
_tmp/

# ============ 私密信息：绝不提交 ============
# 订阅链接、证书私钥、口令等一律不入库。
# 注意：以下按「模式」忽略，而不是按具体路径，这样目录重组后依然生效。
**/.raw/
**/*.raw/

*.local.conf
local.secret
*.p12
*.mobileconfig
*.crt
*.key
*.pfx
# 插件包缓存（extract-plugin.mjs 下载用，417KB，不入库）
_plugin_cache/
_wk/

# 插件包临时文件
_p.plugin
*.plugin

# 临时分析文件（下划线开头）
_*.list
_*.js
_*.json
_*.conf
_*.plugin
_*.snippet
