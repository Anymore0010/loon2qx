# ============ 依赖 / 临时产物 ============
node_modules/
_work/
_tmp/

# ============ 私密信息：绝不提交 ============
# 订阅链接、证书私钥、口令等一律不入库。
# 注意：以下按「模式」忽略，而不是按具体路径，这样目录重组后依然生效。
**/.raw/
**/*.raw/

*.local.conf
local.secret
*.p12
*.mobileconfig
*.crt
*.key
*.pfx
_wk/

# 插件包临时文件
*.plugin

# 临时分析文件 / 模拟目录（下划线开头）。
# 用通用前缀而不是逐个列扩展名：之前按 _*.json / _*.list … 列举，
# 每引入一种新扩展名的临时文件就漏一次（实测漏过 _apns.lsr、_ci/、_work/）。
# 例外见下方 kelee 转换产物（用 ! 显式放行）。
_*


# 本地临时/脚本产物（不属于仓库内容）
__pycache__/
*.pyc
*.tmp
_check.py

# kelee 转换器的辅助产物**必须入库**：
# `_conversion-report.json` 是「哪些规则被跳过/丢弃」的唯一审计来源（逐条记账）。
# git 是「后面的规则覆盖前面」，所以 `!` 必须紧跟在 `_*` 之后。
# ⚠ 路径必须与 tools/convert-plugins.mjs 的实际输出一致 —— 目录调整过两次
# （rules/kelee → rules/rewrite/kelee），例外漏跟就会让报告静默不入库。
!QuantumultX/rules/rewrite/kelee/_conversion-report.json
